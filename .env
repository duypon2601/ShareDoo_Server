; name: Build and Deploy ShareDoo
;
; on:
;   push:
;     branches: [ "main" ]
;
; jobs:
;   build:
;     runs-on: ubuntu-latest
;
;     env: # Khai báo ở cấp job (áp dụng cho toàn bộ job)
;       HUGGINGFACE_API_KEY: ${{ secrets.HUGGINGFACE_API_KEY }}
;
;     steps:
;       - uses: actions/checkout@v3
;
;       - name: Set up JDK 21
;         uses: actions/setup-java@v3
;         with:
;           java-version: '21'
;           distribution: 'temurin'
;           cache: maven
;
;       - name: Build with Maven
;         run: mvn clean install
;
;       # Hoặc truyền biến môi trường chỉ cho 1 step:
;       - name: Run Spring Boot app
;         run: java -jar target/ShareDoo-0.0.1-SNAPSHOT.jar
;         env:
;           HUGGINGFACE_API_KEY: ${{ secrets.HUGGINGFACE_API_KEY }}
